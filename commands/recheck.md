---
description: Re-verify listed claims, observations, expectations, findings, or tickets and flip ticket status; --all replays every claim and reports regressions and fixes
argument-hint: <TKT-nnn | clm-... | --all>
---

Invoke the reality-check skill in `:recheck` mode. Input: $ARGUMENTS

Read `~/.claude/skills/reality-check/SKILL.md` first and follow its `:recheck` rules in the <modes> section and the relevant phases.
